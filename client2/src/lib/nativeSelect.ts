/** Shared styling for unstyled native `<select>`s: room for the menu indicator + `native-select` in `index.css`. */
export const FILTER_SELECT_CLASS =
  'native-select min-h-11 w-full rounded-lg border border-(--border) bg-(--bg) pl-3 pr-10 py-2 text-sm text-(--text-h)';
